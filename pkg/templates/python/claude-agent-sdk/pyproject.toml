[project]
name = "py-claude-agent-sdk"
version = "0.1.0"
description = "Claude Agent SDK browser automation with Kernel"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "kernel>=0.23.0",
    "claude-agent-sdk>=0.1.0",
]

[dependency-groups]
dev = ["mypy>=1.19.0"]
